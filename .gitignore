.env
logs/
*.log
__pycache__/
*.pyc
.DS_Store

# Tooling caches
.serena/
.ruff_cache/
.playwright-mcp/

# Live state pulled off the deploy host for offline analysis — never commit
from-server/

# AV backtest pulled data (raw AV JSON + manifest + results) — never commit
av_data/
scratch_av/
.gdelt_cache/
backtest/gdelt/out/

# Local gold-set scoring runs (scripts/score_gold_set.py --json)
.gold_run*.json
