---
name: external-geo-dashboards-backlog
description: Borrow-backlog from evaluating 5 external geopolitical-news dashboards; work items to address one-per-session (decide/skip counts)
metadata: 
  node_type: memory
  type: project
  originSessionId: 39ea0ce7-2085-49fe-a1c4-254367d03927
  modified: 2026-08-16T11:14:15.365Z
---

2026-06-25: Evaluated 5 external open-source geopolitical-news dashboards (6 parallel research agents) for ideas worth borrowing into news-brief. **All 5 are web/globe dashboards (different shape from our curated Telegram text pipeline) — none to self-host or integrate wholesale.** Value = mining ground for data sources, algorithms, prompt patterns. On the axes that matter to us (claim-level anti-repetition, signal extraction, Telegram delivery) our system is already ahead of theirs.

**Why:** User wants to address each borrowable item one-per-session (even if the session just decides to SKIP), then capture learnings. This is the backlog. Update STATUS per item as we go.

**How to apply:** Each session, pick the next OPEN item, brainstorm/decide/implement-or-skip, mark STATUS, capture learning. Lead with #1 (cheapest win).

LICENSE GUARD: WorldMonitor + Pharos = **AGPL-3.0** → ideas/URL-lists only, reimplement from docs, never lift code. RADAR + Global-News-Intel-Platform + Realpolitik = **MIT** → code-borrowable. (Facts/short URL lists aren't copyrightable regardless.)

## MAIN items (ranked by fit-to-effort)

1. **[DONE+PUSHED 2026-06-25, commits 55a3751..95df755, deploy triggered] Perspective / state-funded source tagging** — shipped LEAN scope: binary `state_funded` everywhere (default False) + OPTIONAL `perspective` (VALID_PERSPECTIVES enum) set only where it changes the read; rendered into the LLM section header (`[REGIONAL · ARAB · STATE-FUNDED]`), taught via SYSTEM_PROMPT (attribute framing to vantage + triangulate agreement/divergence), captured through 2 new /addsource wizard steps. 5 baked feeds tagged (AlJazeera/NHK/Yonhap state-funded; SCMP/Kyiv perspective-only); wires/analysts/think-tanks left untagged by design. KEY DECISION: NO NEUTRAL enum value — absent perspective = "no vantage claim made" (model falls back on priors), NOT a neutrality verdict (a NEUTRAL tag is a positive editorial claim, as contestable as picking a side). Push = Docker deploy = user's call. Spec/plan: docs/superpowers/{specs,plans}/2026-06-25-perspective-state-funded-tagging*.md.

2. **[DONE 2026-06-25: NO-GO / SKIP — well-powered confident null, item CLOSED]** GDELT 2.0 as a TRADING-SIGNAL feeder. Built a throwaway signal-validation spike (`backtest/gdelt/`, commits 719bff4..778701d on main, NOT pushed) reusing the existing `backtest/` IC engine: GDELT 1.0 daily Events → Middle-East conflict daily signal → causal forward-snap → held-out Spearman IC vs USO/GLD/SPY/ITA. Ran 2022-01-01..2024-12-31 (n≈375/cell, 2553 region-days). RESULT = NO-GO: pre-registered gate (correct-sign IC>0.03 + hit>53% on BOTH oil & gold) fails decisively — **oil USO IC = −0.2196 (≈4·SE, WRONG sign, 43% hit)**, gold weak (+0.066, misses hit-rate); only defense ITA (+0.093) is thesis-consistent but a non-gate cell within multiple-comparison noise (24 cells, SE≈0.052). FOLLOW-UPS (4, on cached data, `backtest/gdelt/followups.py`) HARDEN the null: lone strong cell USO −0.22 is NON-STATIONARY (per-year IC flips +0.155/−0.180/−0.006 = regime artifact, full-period ≈0 — a 50/50 temporal split manufactured the scary held-out number); normalize+chokepoint-tighten KILLS the oil edge (Hormuz-states→USO ≈ −0.03); broadened instruments (VIX/BNO/UNG/GDX) no coherent signal. ONE future lead (separate spec, NOT item #2, UNVALIDATED for stationarity, likely 2022-shock-confounded): Russia-Ukraine conflict → commodities all correct-signed (gas UNG hit 58%, wheat WEAT hit 59%, oil USO IC +0.12) — revisit as event-study around discrete shocks, not daily level. Did NOT build the 2.0 real-time feeder (the whole point of validating first). KEY GOTCHAS: (a) simulated-2026 env — used 2022-24 as unambiguously-real window; (b) GDELT files are correctly dated by MODAL SQLDATE, row-0 is a straggler (false 1-yr-shift alarm). Full writeup: `backtest/gdelt/FINDINGS.md`. Spec/plan: docs/superpowers/{specs,plans}/2026-06-25-gdelt-signal-validation-spike*. Original recipe (for reference, NOT pursued): 2.0 floored-15min URL `{YYYYMMDDHHmm00}.export.CSV.zip`, −20min lag, 404=not-ready, route via trading-side seam.

3. **[DONE+PUSHED 2026-06-25, commits 51657ab..90a5d44 on main, f80de1a..90a5d44 → origin/main, Docker deploy triggered]** Multi-source corroboration count → claim ledger. Built as a **reader-facing confidence tag** (NOT a ranking score, NOT retention weighting — both deliberately rejected as YAGNI at ~190 items / the cap+TTL+LLM-selection already soft-weights importance). Each durable claim now carries `source_count: int` kept at its **PEAK** (max across days — "established" = ever broadly confirmed, so the tag doesn't decay as a story ages out). Coarse bucket DERIVED at render time (not stored, so thresholds retune without rewriting history): 1=single-source / 2-3=corroborated / ≥4=widely corroborated / 0=no cue. KEY OBSTACLE solved: the brief runs as two processes hours apart (mode_submit fetches source-labelled feeds → batch; mode_collect reconciles from brief TEXT only, feeds gone). So a titles-only source-labelled index is persisted at submit (`source_index-{day}.json` on the volume) and fed into the EXISTING Haiku reconcile call at collect — the model counts distinct `SOURCE:` blocks supporting each claim (grounded, no 3rd API call). Rejected: reconcile guessing from brief prose (ungrounded → false-confidence tags, "a false widely-corroborated tag is worse than no tag"). Fully fail-safe + back-compat (missing index = today's behaviour, old claims render untagged); rides existing `BRIEF_MEMORY_ENABLED` (on in prod → activates on deploy). 5-task subagent-driven TDD build, reviews inline; +357/-12, 418 passed. Deferred MINOR: source_index-{day}.json accumulates daily (same pattern as brief-/enrichment- files). Spec/plan: docs/superpowers/{specs,plans}/2026-06-25-corroboration-confidence-tag*. Connects to the confidence-calibration thread ([[sentiment-sizing-null-decided]] "next value"). AGPL → reimplemented from WorldMonitor's corroboration idea, no code lifted.

## LOW-PRIORITY items

4. **[DONE+PUSHED 2026-06-26 — commits b260763..af36c89 (+423679d..c49eb39 minors); pushed in batch 6a9fd5d..76d20f8 → origin/main, Docker deploy triggered]** Source-list mining. **REFRAMED during brainstorm** away from "ingest RADAR" toward the higher-leverage gap: the perspective-tagging feature (#1) defined a 10-value vantage enum but only 5 were ever sourced. Shipped = **complete the perspective matrix** + a small energy starter. Added 8 live-validated feeds to baked `RSS_FEEDS` (now 25 total, 9/10 perspectives sourced — only WESTERN unused, by design): RUSSIAN=TASS(state)+Meduza(indep), IRANIAN=Press TV(state)+IranWire(indep), ISRAELI=Times of Israel, INDIAN=The Hindu (state-media countries got BOTH official+independent; free-press got one); energy (no vantage)=OilPrice+EIA. KEY METHOD: one-off scratchpad validator (native RSS first → Google-News `site:` proxy fallback → ≥3-entries gate) run BEFORE baking, so no dead URL ships; survivors carry `# verified N entries` comments. 8/9 candidates validated NATIVE; Reuters Commodities DROPPED (proxy=1 entry, no alternate → energy=2). RADAR ended up NOT load-bearing (canonical outlets are well-known; RADAR = discovery cross-check only; URLs/facts not copyrightable anyway). Subagent-driven (3 tasks, inline reviews per [[subagent-review-stalls]]); 443 tests. MINORS RESOLVED same session (commits 423679d..c49eb39, +1 feed → RSS_FEEDS now 26): commodities trio = OilPrice + EIA + **Mining.com** (native RSS, metals, 36 entries) under renamed category energy→commodities; The Hindu → International section feed (60 entries, was site-wide India-domestic skew). REUTERS COMMODITIES REJECTED after user pushed back on keyword queries: investigation found Google-News `when:` filter is the freshness gate — narrow `site:reuters.com/markets/commodities` path returns only 1 with when:2d/7d (≈0 items dated in last week) but 100 with NO when: (= STALE, defeats the freshness guardrail); a keyword `commodities site:reuters.com`=100 fresh but loose (word-match across whole domain). So a poorly-indexed site:path CANNOT be rescued for fresh news → went native (Mining.com, no proxy/no keyword). Investing.com(10 native) parked. LESSON ties to [[google-news-rss-recipe]]: verify a thin proxy by toggling when: — if no-when≫when, the path lacks RECENT items and the volume is stale. Spec/plan: docs/superpowers/{specs,plans}/2026-06-26-source-mining-perspective-matrix*. Original list refs (unused this pass): RADAR `server/src/news_feeds.json` (MIT, 190+ countries), Pharos `rss-feeds.ts` (AGPL read-only), WorldMonitor `_feeds.ts` (AGPL).

5. **Prompt patterns (MIT, free to adopt).** (a) **[DONE+PUSHED 2026-06-26 — commits 8f3e0ef..ac8a333; pushed in batch 6a9fd5d..76d20f8 → origin/main, Docker deploy triggered]** Realpolitik "fallout prediction" structure → shipped as an **invisible "why it matters" lens**: SYSTEM_PROMPT teaches a 3-beat analytical STANDARD (non-obvious context / next-move+timeframe / market-transmission), woven into prose with NO `Context/Stakes/Connection` labels in output; pointed at TOP STORIES via OUTPUT FORMAT; WATCH/FORWARD gained a one-clause anti-repetition guard. KEY ADAPTATIONS: 3rd beat re-pointed from Realpolitik's "daily life" → **market transmission** (reader is an investor); scope deliberately split — SYSTEM_PROMPT teaches generally (so it can lightly sharpen dynamic sections too, BY DESIGN — not a bug if depth "leaks" into minor sections), OUTPUT FORMAT scopes specifically to TOP STORIES. Pure prompt change, no code/schema; 2 new prompt-substring regression tests (440 pass); per-task + Opus whole-branch review = READY TO MERGE. Unconditional prompt text (no flag-gate) → activates next `mode_submit` once pushed. Spec/plan: docs/superpowers/{specs,plans}/2026-06-26-why-it-matters-lens*. (b) **[DONE+PUSHED 2026-06-26 — commits 3cb2a0e..e130065; pushed in batch 6a9fd5d..76d20f8 → origin/main, Docker deploy triggered]** Severity-weighted retention: importance buys extra TTL. Shipped = model-assigned `severity` (low/normal/high) on each ledger claim, assigned by the EXISTING Haiku reconcile call (stayed Haiku — severity tiering is easier than the source-counting it already does; fail-safe+reversible+one-line `RECONCILE_MODEL` swap if live ledgers strain; upgrading would drag in the Sonnet timeout re-tune per e255436, out of scope). KEY MECHANIC: 3-tier ADDITIVE-ONLY TTL (severity only EXTENDS life, never culls early) → TTLs 7d/7d/14d via a high-only `_ttl_bonus` on an "effective age" (= days-since-reaffirmed − bonus); BOTH eviction gates honor severity but via TWO lookups not one (additive-only forces it: low==normal for TTL but low<normal under cap) — TTL filter uses the high-only bonus, cap sort uses full `(_severity_rank low0/normal1/high2, last_reaffirmed)` desc. Lifecycle: new=default normal; reaffirm=LATEST-VALID-ELSE-KEEP (importance moves both ways but an omitted/invalid severity never demotes; NOT peak-max like source_count, which would ratchet all to high in a week). Back-compat: legacy claims w/ no field → normal everywhere (helpers default). Severity is INTERNAL — deliberately NOT rendered into ESTABLISHED block (that block = "don't re-explain"; surfacing severity risks re-featuring + overlaps why-it-matters lens). brief_memory.py ONLY; descriptive/no-trading; rides live BRIEF_MEMORY_ENABLED. Bounded risk on record: a live high (≤14d) outranks ALL normal/low for cap slots regardless of recency — intended "importance buys life", safe (high rare, self-heals 14d), mis-tier over-suppresses ≤2wk cosmetic-only. 5-task subagent-driven TDD (reviews INLINE per [[subagent-review-stalls]]); 478 passed, ruff clean; final review READY TO MERGE no findings. Spec/plan: docs/superpowers/{specs,plans}/2026-06-26-severity-weighted-retention*. Connects to confidence-calibration thread ([[sentiment-sizing-null-decided]] "next value") + [[brief-claim-memory-build]].

6. **[DONE+PUSHED 2026-06-26 — commits c9cf9be..7abe988; pushed in batch 6a9fd5d..76d20f8 → origin/main, Docker deploy triggered]** Claim-verification-before-publish (Pharos Grok pattern, AGPL idea reimplemented, no code lifted). **REFRAMED as a measurement PILOT, not a feature** (validate-the-premise discipline, mirrors GDELT): user noticed brief makes unsupported/wrong claims "occasionally" but not sure if recurring → build a flag-gated, log-only Sonnet **grounding** check (NOT truth-check: can't cheaply verify truth since batch model already web-searched; CAN cheaply verify "does each TOP STORIES claim trace to source material we fed it"), accumulate ~14d shadow logs, decide promote/keep/kill against a PRE-REGISTERED gate. New module `claim_verify.py` (mirrors brief_memory.py): mode_submit persists richer `claim_evidence-{day}.json` (= source_index BUT retains the ≤400-char RSS summaries build_source_index drops — nearly free, fetch_rss already attaches them); mode_collect AFTER deliver() runs a forced-tool Sonnet call (`claude-sonnet-4-6`, 90s+retry, post-delivery so latency free) → `verification-{day}.json`; `summarize_verifications` aggregates for review. KEY DESIGN: 5-verdict taxonomy supported/unsupported/contradicted/overstated/**unverifiable** — the unverifiable bucket is the noise valve (keeps hedged/forward-looking lines out of the denominator so flag rate means something). CENTRAL CONFOUND (pre-registered): brief web-searches, so a true claim sourced beyond our feeds flags `unsupported` → **lead the gate on `contradicted` (confound-free), treat raw unsupported as needing manual triage**; Gate-0 = instrument-validity (judge precision ≥~50% on flags) before trusting counts; then kill(≈0 confirmed-wrong, most likely)/keep-silent/promote-to-reader-flag. Flag `CLAIM_VERIFY_ENABLED` default-off, independent of BRIEF_MEMORY_ENABLED, fail-safe (never delays/alters delivered brief). 6-task subagent-driven TDD (haiku impl 1-5 transcription, sonnet wiring 6; reviews INLINE per [[subagent-review-stalls]]); 501 passed, ruff clean, final review READY TO MERGE. NOTED FRAGILITY (deferred, not a defect): claim_evidence keyed by submit-day but loaded by collect-day — works ONLY because submit+collect run SAME UTC day (user confirmed); SHARED root with #3 source_index; if schedule ever crosses UTC midnight both silently miss (fix=key load by state["date"]). Spec/plan: docs/superpowers/{specs,plans}/2026-06-26-claim-verification-grounding-pilot*. **Pilot operation: deploy → set CLAIM_VERIFY_ENABLED=1 on host → ~14d → run summarize_verifications → apply gate. ENABLED on the server (user confirmed 2026-08-16; enable date not recorded — check the oldest `verification-*.json` on the volume to date the window).** Remaining: once ~14d of logs exist, run `summarize_verifications` and apply the pre-registered gate — **Gate-0 instrument-validity first** (judge precision ≥~50% on flags), then lead on `contradicted` because raw `unsupported` carries the web-search confound. **← LAST backlog item; geo-dashboards borrow-backlog now CLOSED (all 6 addressed).**

## SKIP explicitly (don't revisit)
Self-hosting any of them; map/globe frontends; warehouse/dbt/Dagster/Redis-seeder infra; full `scoreImportance` ranking pipeline (premature at ~190 items — we deliberately offload ranking to Claude).

## The 5 repos
- WorldMonitor (koala73) — AGPL, ~60k★, actively maintained, richest algorithms doc (worldmonitor.app/docs/algorithms)
- Pharos-ai (Juliusolsson05) / conflicts.app — AGPL, ~145★, **architectural twin** (Claude Sonnet 4.6 agent + Telegram + 30-min heartbeat); same Google-News-`site:` recipe as ours
- Realpolitik (iamjameskeane) — MIT, ~35★, Python worker (Gemini), feed/hotspot dicts withheld
- RADAR (Syntax-Error-1337) — MIT, ~35★, TS map dashboard, no dedup; asset = news_feeds.json
- Global-News-Intel-Platform (Mohith-akash) — MIT, ~19★, GDELT-only Streamlit/MotherDuck warehouse + NL-to-SQL

See [[brief-sources-and-edge-latency-thread]], [[multi-asset-trading-build]], [[brief-claim-memory-build]].
