# Project memory — news-brief

- [📁 Historical milestones](MEMORY-history.md) — the whole Bigdata.com enrichment arc (brainstorm through REST rewrite; feature DONE), the sources & edge-latency redesign, and the multi-asset trading build. NOT auto-loaded; read on demand for past-work context.

- [Trading retrospective 2026-08-16](trading-retrospective-2026-08-16.md) — **distrust every pre-2026-08-16 number**: the performance layer was corrupt. "Fade the event" is DEAD and the reversal rule is removed. **REJECTED on evidence, don't re-propose: the momentum-prediction cap, the BTFDBot parallel sleeve.**
- [Analysis stats traps](analysis-stats-traps.md) — **seven** traps that produced confident wrong numbers, including **an eval set selected by the same signal the strategy used** and **a threshold quoted off the wrong function**. Read before trusting any measurement here.
- [PolyGram live trading spec](polygram-live-trading-spec.md) — **RETIRED 2026-09-11, code removed (oh4): the venue charged ~$1/share at any displayed price, no reachable alternative. Don't re-propose.** Record: `docs/2026-09-11-prediction-trading-retired.md`. Kept for the per-endpoint response-shape and book-arithmetic lessons.
- [Self-improving trading roadmap](self-improving-trading-roadmap.md) — staged A→B→C→D; Stage A BUILT, Stage B NEXT (edit `performance_prompt_block`'s HARDCODED dim tuple, not `_DIMENSIONS`). Prediction class removed 2026-09-11

- [External geo-dashboards borrow-backlog](external-geo-dashboards-backlog.md) — **CLOSED 2026-06-26, all 6 items done + pushed.** The claim-verify pilot was DECIDED 2026-08-16 = keep as a silent log (45 briefs, zero confirmed contradictions). Durable bits: the validate-then-bake ≥3-entries native→proxy gate, and the AGPL-vs-MIT guard
- [Mauldin TWIE Wix-warmup scrape](mauldin-twie-wix-warmup-scrape.md) — 2026-06-27 BUILT+PUSHED (c50ae8a): Jacob Shapiro's "The World Isn't Ending" as always-on baked-in source via parsing Wix `wix-warmup-data` JSON (no RSS on Wix SPAs); article page has records, section page only schema
- [Telegram send-long convention](telegram-send-long-convention.md) — `telegram_send_long` wraps every growth-prone send; bare `telegram_send` is only for fixed-size acks. TEST GOTCHA: there are THREE send channels, and patching fewer lets a "nothing was sent" assertion pass vacuously.
- [Model config + thinking gotcha](newsbrief-model-config.md) — `claude-sonnet-5`, swapped via the `NEWSBRIEF_MODEL` **settings row** (no redeploy). **Set `thinking` EXPLICITLY on every call** — adaptive thinking eats max_tokens and truncates. Audit tight max_tokens after any model bump.
- [Sim-env external data axis](sim-env-external-data-axis.md) — simulated-2026 clock; use pre-2025 (2022-24) windows for unambiguously-real yfinance+GDELT backtests; date GDELT files by MODAL SQLDATE (row-0 is a straggler)
- [Backtest non-stationarity check](backtest-nonstationarity-check.md) — a held-out IC alone isn't proof; per-period (per-year) sign-stability check catches regime artifacts (GDELT USO −0.22 flipped +0.155/−0.180/−0.006 = noise); also normalize + tighten scope
- [Formatter owns style](formatter-owns-style.md) — ruff reflows brief.py on save; don't preserve hand-alignment
- [brief.py sibling prompt strings](brief-py-sibling-prompt-strings.md) — 3 big triple-quoted prompt blocks (SYSTEM_PROMPT / WEEKLY_SYSTEM_PROMPT / build_daily_prompt f-string); "edit the prompt" mis-targets — name the symbol + match unique surrounding text
- [Env var needs compose passthrough](env-var-needs-compose-passthrough.md) — a knob is INVISIBLE in the container until the compose **anchor** declares it, and the anchor SEEDS the settings row, so adding the line later fixes nothing. **Verify the EFFECT, never the row.** The repo's compose is a stale template; the host's is ahead.
- [Dockerfile COPY allowlist](dockerfile-copy-allowlist.md) — a new TOP-LEVEL module needs three updates (Dockerfile COPY + two workflow lists) or it ModuleNotFounds in production while CI stays green. Enforced by `tests/test_packaging.py` — extend those tests, don't hand-maintain a list.
- [Source fetch failure modes](source-fetch-failure-modes.md) — an `RSS failed` warning is 3 unrelated problems: 403 = bot UA (one `SOURCE_USER_AGENT` now; never retry a 403), 429 = Nitter rate-limiting adjacent feeds, SSL = presstv.ir's **REVOKED** cert → replaced by IRNA (don't re-add it, don't disable verification). Reproduce with curl AND requests before fixing
- [Commodity signals are index class](commodity-signals-are-index-class.md) — 2026-08-09: BRENT etc. are asset CLASS `index` (raw Yahoo symbols), reclassified at normalize time, not open time (dedup key is built first). Prediction branch it mentions is gone (oh4)
- [Google News RSS recipe](google-news-rss-recipe.md) — build feeds for dead publisher RSS; use `site:` not `allinurl:`+`when:`; deep section paths can be stale-only (if no-when≫when:2d, go native)
- [Direct-page temp sources](direct-page-temp-sources.md) — 2026-06-25 BUILT+PUSHED (deploy triggered): temp sources can be `source_type=page` (scraped via fetch_web_source), wizard asks feed-or-page; Iran dashboard moved off in-code WEB_SOURCES; PENDING host re-add of both BCA dashboards as page sources
- [Paper sim: no fees (decided)](paper-sim-no-fees-decision.md) — frictionless by design; rejected demo T212 account; haircut path noted
- [T212 401 diagnosis](t212-401-diagnosis.md) — RESOLVED; auth is HTTP Basic base64(KEY_ID:KEY); root cause was literal-placeholder bug
- [Run brief.py locally](brief-local-run.md) — the pre-push gate is **three** commands, not pytest alone, and **`pytest` alone reports green with the whole DB layer SKIPPED**. CI has no pandas. **Place a test by what it NEEDS, not what it's ABOUT.** **Start the test Postgres with `--tmpfs /var/lib/postgresql`** — postgres:18 refuses `.../data`; `--rm` does NOT remove anonymous volumes, and 57 abandoned ones (21GB) filled the disk on 2026-09-10.
- [Deferred review findings](newsbrief-deferred-findings.md) — backlog worked ONE item per session, proper design each; everything through 2026-06-26 RESOLVED. **STILL OPEN, deferred-for-cause:** #2 `split_html_message` tag-split (low-probability), #3 latent `fetch_web_source` `[:800]` fallback (YAGNI)
- [Beads hygiene + the Stop hook](newsbrief-beads-hygiene.md) — a bead id ALONE on a line in a commit means "completes it"; inline prose is a reference. Traps around `bd export` counts, phantom ids from titles, and autocrlf are in the entry.
- [Commit straight to main](newsbrief-commit-to-main.md) — solo repo; don't branch first
- [User runs concurrent sessions](user-runs-concurrent-sessions.md) — unexplained working-tree changes are HIS, from another session; flag and ask, never revert or stash; commit with explicit paths, never `-A`; a foreign change may be only half a feature
- [HTTP error body is the diagnosis](http-error-body-is-the-diagnosis.md) — `HTTPError` stringifies to status+URL only; log `resp.text` (+ the request body on writes) or a 400 is unactionable; ~25 sites still discard it
- [Fail-closed paths need status, not counts](fail-closed-needs-status-not-count.md) — returning `0` from a multi-gate silent path is unattributable; return which gate fired with numbers, render it where the operator looks, and make a swallowed exception a `crashed` state
- [Write-only fields hide from read paths](write-only-fields-hide-from-read-paths.md) — when every GET works and only the POST fails, look for the field no read consumes; a docstring naming a field the code never reads is the marker
- [Signals delimiter fragility](signals-delimiter-fragility.md) — marker is @@@SIGNALS@@@; model mangles markdown-looking markers; parse by structure
- [Signals parse_error = truncation](signals-parse-error-is-truncation.md) — GENERALIZED rule, recurred 4x: any post-gen call's parse or format failure is usually **max_tokens truncation, not a bad parser**. Check `stop_reason` before the parser, and size max_tokens + timeout to the workload
- [Batch pipeline timing](batch-pipeline-timing.md) — submit→collect may span up to 12h but must share a UTC day (per-day artifacts are filename-date-keyed). Crashed submit: a `NameResolutionError` proves NO batch was created, so resubmit freely; otherwise grep the `Batch submitted` line.
- [Stooq ticker resolution](stooq-ticker-resolution.md) — signals carry plain symbols (SHEL); T212 keys are SYMBOL_US_EQ / SYMBOLl_EQ; resolve by base, prefer US
- [Stooq dead → Yahoo+Alpaca](stooq-dead-provider-replacement.md) — Stooq free API permanently removed (404 + PoW wall); SHIPPED 2026-06-15 market-routed Yahoo+Alpaca failover (fetch_quote/fetch_benchmark) + market-pulse revived via "index" asset class w/ raw Yahoo symbols; T212 has no quote endpoint
- [Run Python from the Bash tool](python-via-powershell.md) — **`py` first** (`py -m pytest`, `py script.py`): it returns the real exit code where the winpty-wrapped `python` alias does not. The PowerShell fallback and its traps are in the entry.
- [Live state on deploy host](live-state-on-deploy-host.md) — runtime state lives on the deploy host, NOT the dev repo; **the repo is PUBLIC and `.gitignore` forbids committing live output (`from-server/`)** — check both before proposing any artifact enter git
- [Fix the tooling, don't route around it](fix-the-tooling-dont-route-around-it.md) — a false-firing guard should be FIXED, not worked around; a second wrong firing on the same shape means stop and offer the fix. Verify a hook in BOTH directions with live controls.
- [User working style](user-working-style.md) — doesn't rubber-stamp recommendations; lead with honest tradeoffs, expect to defend "recommended"; sequences work by context budget and audits what gets published
- [Subagent dispatch practices](subagent-review-stalls.md) — **DEFAULT TO DISPATCHING** (2026-09-11: 6 tasks, 12 agents, 0 stalls, 2 one-line fix rounds). Implementers PARK on a background Monitor that never wakes them — read the gate file and nudge with the result; long reviewer returns TRUNCATE — file + path contract. haiku for transcription, sonnet for removals, opus for shared-path reviews. Pre-register absolute counts; a disagreement is REPORTED, not reconciled
- [Fork subagent overreach](fork-subagent-overreach.md) — subagent_type:'fork' inherits full context + runs the whole plan, not your narrow prompt; may misreport scope ("pushed" when local); use fresh agents for scoped tasks + verify git before trusting
- [Flag access = module attr](newsbrief-flag-access-module-attr.md) — read knobs as `common.X`, never `from common import X`: a from-copy freezes at import and defeats both host toggles and monkeypatch. Load-bearing now that knobs are rows behind a PEP 562 `__getattr__`.
- [TDD plan fixtures drift from contracts](tdd-plan-fixtures-drift-from-contracts.md) — FIVE instances: plan-written fixtures use fields/signatures the real fn doesn't; fixture and code can AGREE and both be wrong about an external system (date external-shape fixtures); **a fixture that trips two exclusion clauses discriminates neither** — when adding a clause, re-read every fixture the old clause's test uses; **run pasted test code or label it unrun**
- [Plugin "not cached" = scope mismatch](plugin-scope-mismatch-not-cached.md) — /doctor warning fix: flip install record project→user scope in installed_plugins.json (not a cache problem); 14 plugins fixed 2026-06-19
- [Telegram commands are text-parsed](telegram-commands-text-parsed.md) — handlers run on message text; setMyCommands self-sync + callback_query now built (updated 2026-06-19, see below)
- [Telegram source mgmt + command daemon](telegram-source-management-daemon.md) — SHIPPED 2026-06-19: commands mode is now a real-time long-poll daemon (only getUpdates consumer; 409 if doubled; up -d not cron); temp news sources via /addsource button wizard + /sources, stored in sources.json on the volume (no redeploy)
- [Sentiment-sizing CLOSED null](sentiment-sizing-null-decided.md) — 2026-06-24 DECISION: sentiment does NOT size positions (confident null, n≈7k); enrichment permanently descriptive-only; bigdata REST key repurposed to in-depth-brief depth (research offload), faithful sizing-backtest dropped; next value = brief quality (topic-novelty filter, confidence calibration)
- [TradingAgents patterns to mine](tradingagents-patterns-to-mine.md) — 2026-06-24: TauricResearch/TradingAgents NOT adopted; mine bull/bear-debate + risk-manager stage + its AlphaVantage wiring in a SEPARATE later spec (trading/signals subsystem)
- [Brief claim-memory build](brief-claim-memory-build.md) — 2026-06-24 BUILT+PUSHED (commits 09e1b9e..3e6b4fb, 355 tests): anti-repetition feature; repetition is claim-level (standing facts re-stated daily) NOT topic-level; (A) un-truncate yesterday LIVE + (B) standing-claim ledger ENABLED on host 2026-06-25 (first live ledger at 2026-06-26 collect); separate post-gen reconcile call, fail-safe
- [Signals extraction → own call](signals-extraction-separate-call-followup.md) — 2026-06-25 BUILT+PUSHED: signals moved to a separate forced-tool call. LESSON: match a post-gen call's timeout to its model's latency — a timeout copied from a faster model silently wiped signals.
- [PolyGram candidate-search fix](polygram-candidate-search-fix.md) — RETIRED with the feature (oh4, 2026-09-11); the durable part is the search lesson: case-insensitive SUBSTRING matching makes short tokens toxic (MU→Musk), so search entity tokens of ≥4 chars.
- [KB architecture redirection 2026-08-29](newsbrief-kb-architecture-2026-08-29.md) — brief becomes a render over an accumulated KB. **Epic 1 CLOSED 15/15 and PUSHED.** The rule the epic converged on: **quarantine is the default for an unmeasured field; measurement lifts it.** Follow-up `6wc` — ledger storage unbounded in time.
- [KB schema 0006 + the ledger cutover 0007](newsbrief-kb-schema-0006.md) — the KB's 16 tables plus the claim ledger in Postgres. **Read before touching the KB schema or `claim_store.py`, which owns all its SQL.** Retirement is a `retired_on` date, **never a DELETE**.
- [Runtime foundation, phases 1 and 2](newsbrief-runtime-foundation-phase-1.md) — Epic 7 CLOSED. **All configuration is `settings` ROWS**: a new knob = a `KNOBS` entry + deleting the constant + a compose anchor line. **The unpushed-hold is OVER — do not re-apply it.**
- [Continuous capture (Epic 2, b42.1)](newsbrief-capture-feature.md) — **ON IN PRODUCTION since 2026-09-04.** `capture.py` polls 26 feeds every 30 min as a job child. **`captured_items` is DEAD, don't build it**; `items` has NO feed column BY DESIGN. **Carries the REFERENCE ALERT CONTRACT (`capture.liveness`) every other alert here copies.** Open: `b42.5` (per-feed intervals).
- [Comprehend cost baseline](newsbrief-comprehend-cost.md) — **the 2026-09-25 £15/day was INPUT VOLUME (Reuters quote pages), not per-item cost: check per-outlet counts first.** ~$31 for 7–10 Sep, 57% of it ONE spike day already fixed. **DEAD IDEAS: truncating bodies (avg 154 chars) and caching the 777-token prefix (below Sonnet 5's 1024 minimum — accepted, silently inert).** Full doc: `docs/2026-09-10-comprehend-cost-handover.md`
- [Comprehension cost redesign, phase 1](comprehension-cost-redesign-phase-1.md) — **BUILT AND PUSHED 2026-09-25, host runbook NOT run.** `COMPREHEND_ENABLED` stays OFF until the old gate runs, on or after 2026-09-30 00:13:37Z. Run `docs/2026-09-25-host-runbook-cost-redesign-phase-1.md` in order. The triage key is `NEWSBRIEF_TRIAGE_MODEL`, not `TRIAGE_MODEL`.
- [Snapshot config per unit of work](snapshot-config-per-unit-of-work.md) — a knob re-read through the 60 s settings cache at every use can change MID-PASS. A model switch bypassed the ledger and the budget. Resolve it once per pass and thread it through.
- [Comprehension pipeline (bqa.4b)](newsbrief-comprehension-pipeline.md) — LIVE. Ranking is pg_trgm similarity (`9c40935`): **44% batched recall@30 vs entity overlap's 27.5%, z=4.98.** **§8.2's floor is 10%, the measured rate ~6.4% and DRIFTING DOWN as the corpus grows, so waiting makes the gate harder.** **`corroboration_by_outlet` CANNOT A/B a ranking change at this size** — attribute with `recall@30`. `bqa.11` is blocked on the `yxd` decision: `docs/2026-09-14-corroboration-gate-decision.md`.
- [Reconstruction drifts from production](reconstruction-drifts-from-production.md) — a diagnostic that REIMPLEMENTS production's query stops mirroring it the moment production changes, and **a control comparing two copies of the same logic cannot notice.** Call the production function with an `as_of`.
- [A ledger dates what it records](a-ledger-dates-what-it-records.md) — a timestamp ledger dates ITS OWN events, never the thing you want dated; `schema_migrations.applied_at` used as a code cutover **predated the change by 3h27m**. **Put the precondition in the OUTPUT next to the number, and make an unanswerable instrument REFUSE.**
- [Never give a model raw database ids](never-give-a-model-raw-database-ids.md) — real primary keys as reference tokens let an invented id collide with a real row; use an opaque label space (`ENT1`). **The obvious fix — ignore unknown ids and fall through — is the trap**; rejecting instead deadlocks the cold start
- [Retry budgets need a verdict](retry-budget-needs-a-verdict.md) — a counter that PERMANENTLY retires work may only be charged by failures that **judged** that work: timeout/429/5xx defer, 4xx charges. Count deferrals in their OWN field, and **never put two no-verdict causes on one counter.**
- [Shared helper carries its first caller's tuning](shared-helper-carries-first-callers-tuning.md) — a constant whose comment explains *why this value is safe here* is scoped to ONE caller; a second caller must re-derive it. And **retry layers MULTIPLY** — put the retry where budget accounting lives.
- [Mutation diagnostics demand a COUNT](mutation-diagnostic-demands-a-count.md) — make the worker BREAK the code and report **how many** tests failed, not whether one did: it audits the TESTS. Pre-register N MINUS the silence-assertions. **A boundary test stops being one when the boundary MOVES.** A `-k` filter can exclude the test being mutated, and adding a test invalidates earlier counts on the same lines.
- [Tests asserting less than their name](tests-asserting-less-than-their-name.md) — **the dominant defect class here** (seven in one run); each described a PROPERTY and asserted the easiest PROXY. The technique that finds them: **mentally delete the code and ask whether the test still passes.** Greppable tell: a test double that accepts a parameter and never asserts it. **2026-09-25: a FIXTURE where right and wrong code look identical — `state_store` aliases the stored dict, and equal knob defaults make two call sites the same value.**
- [Test network guard](test-network-guard.md) — the suite blocks non-loopback sockets AND fails any test that caused a blocked call, swallowed or not; how to stub, when the `allow_blocked_network` marker is legitimate, and the nine real violations it found on day one
- [Memory corpus travels now](memory-corpus-does-not-travel.md) — **RESOLVED 2026-09-04 (`iad`)**: `hydrate-memory.sh` INJECTS the committed index where the live corpus is absent (it does NOT copy files). Write learnings to the projects path, then flush and **review the diff — the repo is PUBLIC.**
