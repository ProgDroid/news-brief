# Project memory — news-brief

- [📁 Historical milestones](MEMORY-history.md) — the whole Bigdata.com enrichment arc (brainstorm through REST rewrite; feature DONE), the sources & edge-latency redesign, and the multi-asset trading build. NOT auto-loaded; read on demand for past-work context.

- [Trading retrospective 2026-08-16](trading-retrospective-2026-08-16.md) — **distrust every pre-2026-08-16 number**: the performance layer was corrupt (3 bugs, one of them the model's own feedback block). Signals are 50/50 directional (p=1.000), so "fade the event" is DEAD and the reversal rule is removed. **REJECTED on evidence, don't re-propose: the momentum-prediction cap and the BTFDBot parallel sleeve.** Host repair done, nothing outstanding; write-up in repo `docs/2026-08-16-trading-retrospective.md`
- [Analysis stats traps](analysis-stats-traps.md) — two significance traps that both fired on the paper book: correlated observations (105/130 closes are reversal legs of one engagement → p=0.033 became p=1.000 once collapsed to ticker-episodes, grouped by INSTRUMENT not ticker string), and ties counted as failures in a paired sign test; prefer paired within-unit designs
- [PolyGram live trading spec](polygram-live-trading-spec.md) — two-sleeve prediction feature. **LIVE TRADING WORKS ON THE SERVER since 2026-08-16, so the "$2 round trip" action is CLOSED — don't re-propose it.** **The paper book does NOT preview live behaviour**: only 24.6% of real paper entries clear Sleeve A's band + spread gate. NEXT = read a week of skip tallies, then spec #2 (machine-proposed theses).
- [Self-improving trading roadmap](self-improving-trading-roadmap.md) — staged A→B→C→D plan; autonomy is earned as paper data validates each step. Stage A (measurement/attribution) BUILT + PUSHED, descriptive with zero autonomy. **Stage B is NEXT** — the firewall is structural: edit `performance_prompt_block`'s HARDCODED dim tuple, not `_DIMENSIONS`

- [External geo-dashboards borrow-backlog](external-geo-dashboards-backlog.md) — **CLOSED 2026-06-26, all 6 items done + pushed.** The claim-verify pilot was DECIDED 2026-08-16 = keep as a silent log (45 briefs, zero confirmed contradictions). Durable bits: the validate-then-bake ≥3-entries native→proxy gate, and the AGPL-vs-MIT guard
- [Mauldin TWIE Wix-warmup scrape](mauldin-twie-wix-warmup-scrape.md) — 2026-06-27 BUILT+PUSHED (c50ae8a): Jacob Shapiro's "The World Isn't Ending" as always-on baked-in source via parsing Wix `wix-warmup-data` JSON (no RSS on Wix SPAs); article page has records, section page only schema
- [Telegram send-long convention](telegram-send-long-convention.md) — `telegram_send_long` (split + throttle + fail-aware) wraps every growth-prone site (weekly, trade-update, `/positions`, monitor); bare `telegram_send` is only for fixed-size acks. TEST GOTCHA: there are THREE send channels — patch `brief.telegram_send`, `common.telegram_send` AND `brief.telegram_send_buttons`, or a "nothing was sent" assertion passes while the message reaches Telegram
- [Model config + thinking gotcha](newsbrief-model-config.md) — `claude-sonnet-5`, swapped via the `NEWSBRIEF_MODEL` **settings row** (~60s, no redeploy). **`brief.SIGNALS_MODEL` and `claim_verify.VERIFY_MODEL` no longer exist — don't grep for them.** **Set `thinking` EXPLICITLY on every call**: Sonnet 5 runs ADAPTIVE thinking when omitted, which eats max_tokens and truncates. **Audit every tight fixed max_tokens after a model bump.**
- [Sim-env external data axis](sim-env-external-data-axis.md) — simulated-2026 clock; use pre-2025 (2022-24) windows for unambiguously-real yfinance+GDELT backtests; date GDELT files by MODAL SQLDATE (row-0 is a straggler)
- [Backtest non-stationarity check](backtest-nonstationarity-check.md) — a held-out IC alone isn't proof; per-period (per-year) sign-stability check catches regime artifacts (GDELT USO −0.22 flipped +0.155/−0.180/−0.006 = noise); also normalize + tighten scope
- [Formatter owns style](formatter-owns-style.md) — ruff reflows brief.py on save; don't preserve hand-alignment
- [brief.py sibling prompt strings](brief-py-sibling-prompt-strings.md) — 3 big triple-quoted prompt blocks (SYSTEM_PROMPT / WEEKLY_SYSTEM_PROMPT / build_daily_prompt f-string); "edit the prompt" mis-targets — name the symbol + match unique surrounding text
- [Env var needs compose passthrough](env-var-needs-compose-passthrough.md) — a knob is INVISIBLE in the container until `docker-compose.yml`'s **anchor** declares it, and the anchor SEEDS the settings row, so adding the line later fixes nothing; on an established host the row is the only path. The anchor name must equal the KNOBS key (enforced by `tests/test_packaging.py`). **Verify the EFFECT, never the row.** The repo's compose is a stale template and the HOST's is ahead — ask him. Three variants in the file.
- [Dockerfile COPY allowlist](dockerfile-copy-allowlist.md) — new TOP-LEVEL module needs 3 updates (Dockerfile COPY + workflow paths: + workflow ruff file lists), else runtime ModuleNotFound / escapes CI lint (CI tests pass on full checkout). **Now ENFORCED by `tests/test_packaging.py` (2026-09-02) after `config.py` broke production by missing all three** — extend those tests, don't hand-maintain a list; verify in the built image (`ENTRYPOINT` takes the MODE only)
- [Source fetch failure modes](source-fetch-failure-modes.md) — an `RSS failed` warning is 3 unrelated problems: 403 = bot UA (one `SOURCE_USER_AGENT` now; never retry a 403), 429 = Nitter rate-limiting adjacent feeds, SSL = presstv.ir's **REVOKED** cert → replaced by IRNA (don't re-add it, don't disable verification). Reproduce with curl AND requests before fixing
- [Commodity signals are index class](commodity-signals-are-index-class.md) — 2026-08-09: "no instrument for BRENT (equity)" is a wrong asset CLASS not a missing symbol (schema enum only offers equity/crypto; pricing already routed `index`→raw Yahoo, only `mode_paper` lacked the branch); reclassify at normalize time, NOT at open time (dedup key is built first)
- [Google News RSS recipe](google-news-rss-recipe.md) — build feeds for dead publisher RSS; use `site:` not `allinurl:`+`when:`; deep section paths can be stale-only (if no-when≫when:2d, go native)
- [Direct-page temp sources](direct-page-temp-sources.md) — 2026-06-25 BUILT+PUSHED (deploy triggered): temp sources can be `source_type=page` (scraped via fetch_web_source), wizard asks feed-or-page; Iran dashboard moved off in-code WEB_SOURCES; PENDING host re-add of both BCA dashboards as page sources
- [Paper sim: no fees (decided)](paper-sim-no-fees-decision.md) — frictionless by design; rejected demo T212 account; haircut path noted
- [T212 401 diagnosis](t212-401-diagnosis.md) — RESOLVED; auth is HTTP Basic base64(KEY_ID:KEY); root cause was literal-placeholder bug
- [Run brief.py locally](brief-local-run.md) — import-safe; full pre-push gate = ruff check + ruff format --check + pytest (NOT pytest alone); stage all reformatted files or CI fails. **`pytest` alone now reports green with the whole DB layer SKIPPED** — export `DATABASE_URL` and check the run count moved **CI has NO pandas** — guard pandas-using tests with `importorskip`. **Place a test by what it NEEDS, not what it's ABOUT**: the comprehend files carry a module-level DB `pytestmark`, so a non-DB test dropped in one never runs in CI.
- [Deferred review findings](newsbrief-deferred-findings.md) — backlog worked ONE item per session, proper design each; everything through 2026-06-26 RESOLVED. **STILL OPEN, deferred-for-cause:** #2 `split_html_message` tag-split (low-probability), #3 latent `fetch_web_source` `[:800]` fallback (YAGNI)
- [Commit straight to main](newsbrief-commit-to-main.md) — solo repo; don't branch first
- [User runs concurrent sessions](user-runs-concurrent-sessions.md) — unexplained working-tree changes are HIS, from another session; flag and ask, never revert or stash; commit with explicit paths, never `-A`; a foreign change may be only half a feature
- [HTTP error body is the diagnosis](http-error-body-is-the-diagnosis.md) — `HTTPError` stringifies to status+URL only; log `resp.text` (+ the request body on writes) or a 400 is unactionable; ~25 sites still discard it
- [Fail-closed paths need status, not counts](fail-closed-needs-status-not-count.md) — returning `0` from a multi-gate silent path is unattributable; return which gate fired with numbers, render it where the operator looks, and make a swallowed exception a `crashed` state
- [Write-only fields hide from read paths](write-only-fields-hide-from-read-paths.md) — when every GET works and only the POST fails, look for the field no read consumes; a docstring naming a field the code never reads is the marker
- [Signals delimiter fragility](signals-delimiter-fragility.md) — marker is @@@SIGNALS@@@; model mangles markdown-looking markers; parse by structure
- [Signals parse_error = truncation](signals-parse-error-is-truncation.md) — GENERALIZED rule, recurred 4x: any post-gen call's parse or format failure is usually **max_tokens truncation, not a bad parser**. Check `stop_reason` before the parser, and size max_tokens + timeout to the workload
- [Batch pipeline timing](batch-pipeline-timing.md) — collect polls up to 12h, so the submit→collect cron gap isn't load-bearing; per-day artifacts (source_index/claim_evidence) are filename-date-keyed → submit+collect must share a UTC day. **Crashed submit: a `NameResolutionError` proves NO batch was created (getaddrinfo failed, nothing sent) — resubmit freely. Otherwise grep for the `Batch submitted` log line, which fires BEFORE `save_state`.** Rerun one mode with `docker compose run --rm newsbrief <mode>`; there is no resume point, so enrichment units are re-spent
- [Stooq ticker resolution](stooq-ticker-resolution.md) — signals carry plain symbols (SHEL); T212 keys are SYMBOL_US_EQ / SYMBOLl_EQ; resolve by base, prefer US
- [Stooq dead → Yahoo+Alpaca](stooq-dead-provider-replacement.md) — Stooq free API permanently removed (404 + PoW wall); SHIPPED 2026-06-15 market-routed Yahoo+Alpaca failover (fetch_quote/fetch_benchmark) + market-pulse revived via "index" asset class w/ raw Yahoo symbols; T212 has no quote endpoint
- [Run Python from the Bash tool](python-via-powershell.md) — **`py` first**: `py -m pytest`, `py script.py` run under Bash and return the real exit code (winpty wraps the `python` alias, not the `py` launcher). Only when you need PowerShell itself: write the `.ps1` with the Write tool (a Bash heredoc containing "python" is guard-denied), pass Windows-form paths, and end it with `REAL_EXIT=$LASTEXITCODE`; PS wraps Python stderr as a scary NativeCommandError even on success
- [Live state on deploy host](live-state-on-deploy-host.md) — runtime state lives on the deploy host, NOT the dev repo; **the repo is PUBLIC and `.gitignore` forbids committing live output (`from-server/`)** — check both before proposing any artifact enter git
- [Fix the tooling, don't route around it](fix-the-tooling-dont-route-around-it.md) — 2026-09-01 correction: he interrupted to say a false-firing guard should be FIXED, not worked around. Second wrong firing on the same shape = stop and offer the fix. Find the LIVE plugin cache copy (a committed fix can sit unapplied for days), check for an existing known-fail note first, and verify a hook in BOTH directions with live controls
- [User working style](user-working-style.md) — doesn't rubber-stamp recommendations; lead with honest tradeoffs, expect to defend "recommended"; sequences work by context budget and audits what gets published
- [Subagent dispatch practices](subagent-review-stalls.md) — **DEFAULT TO DISPATCHING**; stalls are rare (24/24 completed 2026-09-02, 4/4 on 2026-07-19) — this line used to say the opposite and was stale. Give ABSOLUTE expected test counts not deltas; say `git commit -F` up front; state which tests must FAIL *and* which must PASS; the old "pytest via PowerShell" advice is superseded by `py`. Keep per-task base SHAs
- [Fork subagent overreach](fork-subagent-overreach.md) — subagent_type:'fork' inherits full context + runs the whole plan, not your narrow prompt; may misreport scope ("pushed" when local); use fresh agents for scoped tasks + verify git before trusting
- [Flag access = module attr](newsbrief-flag-access-module-attr.md) — knobs must be read as `common.X`, never `from common import X` (a from-copy freezes at import → defeats host toggles + test monkeypatch). **Now load-bearing, not just preferred: knobs are rows behind a PEP 562 `__getattr__`.** Fired again 2026-09-02 in a package `__init__` re-export — check those specifically; plus the `monkeypatch.setattr` leak any new forwarding module must be swept for in conftest
- [TDD plan fixtures drift from contracts](tdd-plan-fixtures-drift-from-contracts.md) — these hand-written TDD plans verify FUNCTIONS called but their test fixtures/assertions can use fields the real fn doesn't (Sleeve A hit 3: missing `question` key, unbound `items`, `_stats` scores net_return not realized_return); check fixtures vs real symbol bodies, fix test to real contract preserving intent, flag in commit
- [Plugin "not cached" = scope mismatch](plugin-scope-mismatch-not-cached.md) — /doctor warning fix: flip install record project→user scope in installed_plugins.json (not a cache problem); 14 plugins fixed 2026-06-19
- [Telegram commands are text-parsed](telegram-commands-text-parsed.md) — handlers run on message text; setMyCommands self-sync + callback_query now built (updated 2026-06-19, see below)
- [Telegram source mgmt + command daemon](telegram-source-management-daemon.md) — SHIPPED 2026-06-19: commands mode is now a real-time long-poll daemon (only getUpdates consumer; 409 if doubled; up -d not cron); temp news sources via /addsource button wizard + /sources, stored in sources.json on the volume (no redeploy)
- [Sentiment-sizing CLOSED null](sentiment-sizing-null-decided.md) — 2026-06-24 DECISION: sentiment does NOT size positions (confident null, n≈7k); enrichment permanently descriptive-only; bigdata REST key repurposed to in-depth-brief depth (research offload), faithful sizing-backtest dropped; next value = brief quality (topic-novelty filter, confidence calibration)
- [TradingAgents patterns to mine](tradingagents-patterns-to-mine.md) — 2026-06-24: TauricResearch/TradingAgents NOT adopted; mine bull/bear-debate + risk-manager stage + its AlphaVantage wiring in a SEPARATE later spec (trading/signals subsystem)
- [Brief claim-memory build](brief-claim-memory-build.md) — 2026-06-24 BUILT+PUSHED (commits 09e1b9e..3e6b4fb, 355 tests): anti-repetition feature; repetition is claim-level (standing facts re-stated daily) NOT topic-level; (A) un-truncate yesterday LIVE + (B) standing-claim ledger ENABLED on host 2026-06-25 (first live ledger at 2026-06-26 collect); separate post-gen reconcile call, fail-safe
- [Signals extraction → own call](signals-extraction-separate-call-followup.md) — 2026-06-25 DECIDED+BUILT+PUSHED (commits 6ef30b1..d103802): @@@SIGNALS@@@ → separate Sonnet 4.6 forced-tool post-gen call; deliver-first; dead split parser removed. REGRESSION FIXED e255436: timeout=30 (copied from Haiku) too short for Sonnet → extract_error wiped signals; now 90s+retry. LESSON: match a post-gen call's timeout to its model's latency
- [PolyGram candidate-search fix](polygram-candidate-search-fix.md) — 2026-06-25 FIXED (4b526dc) why prediction trades never opened: gather searched kebab topic slugs; PolyGram /search is case-insensitive SUBSTRING match (short tokens toxic: MU→Musk). Now entity-token search (≥4 chars, no ticker, volume-ranked, per-query cap). CONFIRMED end-to-end: 2 prediction positions opened post-deploy. Positions are PAPER (live exec = future want)
- [KB architecture redirection 2026-08-29](newsbrief-kb-architecture-2026-08-29.md) — brief becomes a render over an accumulated KB. **Epic 1 CLOSED 15/15 and PUSHED** (admission guard + restatement guard; 100% precision on admissible rows across 8 runs, recall low — which makes lifting the quarantine safer, not riskier). Rule the epic converged on: **quarantine is the default for an unmeasured field; measurement lifts it.** Follow-up `6wc` — ledger storage unbounded in time
- [KB schema 0006 + the ledger cutover 0007](newsbrief-kb-schema-0006.md) — the KB's 16 tables plus the claim ledger in Postgres. **Read before touching the KB schema or `claim_store.py`, which owns all its SQL.** Retirement is a `retired_on` date, **never a DELETE**. **The strict-xfail is GONE — don't look for it.** Traps: `next_ledger_num` must have no `WHERE`; NULL `last_reaffirmed` is a hard error; `ON DELETE RESTRICT` raises 23001, not 23503.
- [Runtime foundation, phases 1 and 2](newsbrief-runtime-foundation-phase-1.md) — Epic 7 CLOSED. **All configuration is `settings` ROWS**: adding a knob = a `KNOBS` entry + deleting the constant + a compose anchor line. **The unpushed-hold is OVER — do not re-apply it.** Two rules it converged on: **a guard must test the exact predicate its consumer reads**, and **a knob tracking another knob defaults to `""`, never a copy of the value.**
- [Continuous capture (Epic 2, b42.1)](newsbrief-capture-feature.md) — **ON IN PRODUCTION since 2026-09-04, so the b42.2 data window starts there.** `capture.py` polls 26 feeds every 30 min as a job child. **`captured_items` is DEAD, don't build it**; `items` has NO feed column BY DESIGN; `feed_polls` is the roll-off denominator; **`record_sightings` and `record_poll` MUST commit in one transaction**. Open: capture has NO alerting path — my spec wrongly claimed it did.
- [Comprehension pipeline (bqa.4b)](newsbrief-comprehension-pipeline.md) — **PUSHED (corrected 2026-09-07), still DISABLED and not deployed** (`COMPREHEND_ENABLED` off); `comprehend.py` + the pre-registered gate, migrations 0009/0010. Five defects that nearly shipped silently, all found by running or mutating code, none by reading. **Recurring shape, 3x: a row a predicate keeps re-selecting that nothing advances.** **A pre-registered gate is one-shot: fix bugs that distort what it READS before flipping the flag** — `wvt`+`uer` were gate-validity bugs, now FIXED; `3wb`/`ya4` correctly deferred. **NEXT = `news-brief-bqa.11`** (enable on the host, pick a window, run the gate; `115`+`ya4` blocked on it). **bd state is LOCAL-ONLY — no `refs/dolt/*` on the remote and no `bd sync` command; `bd export -o .beads/issues.jsonl` is the only travelling record, and it had gone a week stale.**
- [Shared helper carries its first caller's tuning](shared-helper-carries-first-callers-tuning.md) — a constant whose comment explains *why this value is safe here* is scoped to ONE caller; a second caller must re-derive it. And **retry layers MULTIPLY** (3 item-level × 2 HTTP = 6 charged 8192-token generations) — put the retry where budget accounting lives. Widen shared functions with keyword params defaulting to the incumbent + a positive-control test; make a guessed value a knob and log the real duration
- [Mutation diagnostics demand a COUNT](mutation-diagnostic-demands-a-count.md) — make the worker BREAK the code and report **how many** tests failed, not whether one did. It audits the TESTS, and it caught four of my own errors — including a collision test defeated by the collision, and two fixes no test could distinguish from their absence
- [Tests asserting less than their name](tests-asserting-less-than-their-name.md) — **SEVEN vacuous tests in one run**, the dominant defect class here; not one was caught by a failing test. Each described a PROPERTY and asserted the easiest PROXY. The technique that finds them: **mentally delete the code and ask whether the test still passes**. Any absence-assertion needs a presence-sibling; prefer independently-derived expected values; `test_scheduler.py:259` is the self-maintaining template
- [Test network guard](test-network-guard.md) — the suite blocks non-loopback sockets AND fails any test that caused a blocked call, swallowed or not; how to stub, when the `allow_blocked_network` marker is legitimate, and the nine real violations it found on day one
- [Memory corpus travels now](memory-corpus-does-not-travel.md) — **RESOLVED 2026-09-04 (`iad`)**: corpora synced (68 files, identical), `.claude/hooks/hydrate-memory.sh` INJECTS the committed index where the live corpus is absent (it does NOT copy files — the Linux project-key form is unverified), `scripts/flush-memory.sh` is manual because the repo is PUBLIC. Still write learnings to the projects path, then flush + review the diff
