---
name: bigdata-next-steps
description: "Two open follow-ups for the Bigdata.com enrichment work, to pick up in a later session (as of 2026-06-20)"
metadata: 
  node_type: memory
  type: project
  originSessionId: b60250e5-457e-471f-ae28-1f43f2dc9400
---

Open follow-ups after Feature A shipped (2026-06-20). Context: [[bigdata-enrichment-shipped]] (built, dark) and [[bigdata-evaluation-and-trading-split]] (decision/eval).

**STATUS 2026-06-20: Step 2 DONE** — see [[bigdata-enrichment-validated-live]] (flag-on path validated end-to-end with real MCP data; gap #1 field names de-risked; 2 universe bugs found → [[enrichment-universe-bugs]]). **Step 1 (below) is the remaining work**, plus the business-email REST key.

**1. Feature B — sentiment backtest. PLAN WRITTEN 2026-06-20** at `docs/superpowers/plans/2026-06-20-bigdata-sentiment-backtest.md` (writing-plans skill). Decides whether Bigdata.com sentiment/events may drive position *sizing* (Feature A ships regardless). Design from the spec (`docs/superpowers/specs/2026-06-19-bigdata-integration-and-backtest-design.md`): sentiment level/Δ → rank IC + quantile + hit rate + event-conditioned slice; 1d–3mo horizon sweep; split-sample/held-out discipline.

**PROBE FINDING 2026-06-20 (retired the #1 feasibility risk with a decisive NO):** the spec's assumed path — reconstruct historical sentiment from time-windowed `bigdata_search` — DOES NOT WORK. `bigdata_search` results expose NO numeric sentiment (only chunk text + relevance, confirmed smart+fast mode); the sentiment FILTER is per-chunk forecasted-market-impact (not entity tone) with no totals → biased + uncomputable; the tearsheet (which has numeric sentiment+baseline+z) is current-only (90d lookback); the MCP connector has NO historical-sentiment time-series tool. Timestamp windowing works (archive ≥9mo deep), ~1.2 query units/windowed search. **Therefore the faithful series needs RavenPack's purpose-built historical-sentiment dataset via REST → the business-email REST key is now a HARD prerequisite for the faithful backtest, not just a production last-mile.**

**Plan structure (consequence):** Tasks 1–7 = vendor-agnostic engine (series/returns/metrics/evaluation/sources-seam/align/runner), pure-stdlib, fully TDD on synthetic fixtures, buildable NOW (offline-only `backtest/` package — NOT shipped in the cron image, no Dockerfile/CI change). Task 8 = interim MCP-approx pilot (windowed retrieval + Claude-scored tone → DIRECTIONAL-ONLY series; start 5–10 names before fan-out). Task 9 = faithful REST RavenPack-Analytics source, DEFERRED + gated on the key (discovery-first, no guessed code).

**ENGINE BUILT + HUMAN-REVIEWED + PUSHED 2026-06-20 (Tasks 1–7 DONE, on main + origin).** 12 commits `0f110eb`..`a3d669c` then review fix `9c7a252`; **295 tests green; ALL on origin/main**. `backtest/` package: `series.py` (SentimentSeries/PriceSeries + loaders), `returns.py` (forward_returns, positional offsets), `prices_yf.py` (yfinance adapter — local import only, NOT in CI), `metrics.py` (spearman_rank_ic/quantile_returns/hit_rate, pure stdlib), `evaluation.py` (split_pairs/best_horizon/bonferroni_note — NOT validation.py which is the go-live gate), `sources.py` (SentimentSource seam + FixtureSentimentSource reading `sentiment_<TICKER>.json`), `align.py` (align/to_delta/event_filtered — event_filtered REQUIRES date-sorted input), `run.py` (run_backtest pools pairs across tickers per horizon + report_markdown).

**INLINE REVIEW (2026-06-20, by main agent — NOT just the fork's self-review):** verified gate + read every backtest module + ran the enrichment flag-on path myself. Math sound (rank IC tie-correct, forward returns lookahead-clean), probe conclusion evidence-based. **One Important finding FULLY FIXED (`9c7a252` label → `1085fd4` proper fix):** `run_backtest` had picked `best_horizon` by max|IC| over swept horizons on the FULL pooled sample and reported that horizon's IN-SAMPLE IC, with `split_pairs` defined+tested but NOT wired in. `1085fd4` wires it: pools `(date, sentiment, return)` across tickers, sorts by date, splits TEMPORALLY (earlier=discovery, later=held-out), selects the horizon on discovery only, and reports the full metric suite on the held-out set as the out-of-sample confirmation; `report_markdown` shows discovery (in-sample/selection-biased) + held-out confirmation as separate sections. Added `align_dated` (align delegates). Decisive temporal test (ticker A late/+corr, B early/−corr, A inserted first → discovery IC<0, holdout IC>0) proves the split is by date, not ticker order. **Open Minors still to triage when Task 8 starts:** (b) `event_filtered` built+tested but not called by run_backtest (event-conditioned slice not in the go/no-go path); (c) cross-ticker pooling of raw sentiment levels w/ no per-ticker standardization (rank IC mitigates); (d) **fixture theme coverage incomplete** — `theme_European equities`/`Polish equities`/`Japan equities and yen` were NOT captured (3 of 8 themes render empty; symbols complete = 7/7); (e) `prices_yf.fetch_price_series` may break on yfinance MultiIndex columns for single-symbol downloads (operator-run, untested vs live). Plus fork's own deferred Minors (series immutability, quantile q<1, hit_rate 0.0-vs-NaN). **Remaining: Task 8 (operator-run MCP-approx pilot) + Task 9 (REST source, gated on the business-email key → start with the discovery step when it lands).**

**TASK 8 DONE 2026-06-20 (lean wiring pass; commits `0e17b57` scorer+runbooks, `cf29a4b` pilot run; on origin/main).** Phase A: `backtest/scorer_llm.py` (TDD'd pure `build_scoring_prompt`/`parse_score` + live `score_window` haiku shell, not in CI) + `backtest/pilot/{README,capture_windows}.md`. Phase B (operator-run): captured 36 windowed `bigdata_search` results — MU/CVX/RGLD × 12 monthly windows (2025-04..2026-03), ~31 query units (~$0.23); archive reached back to 2025-04 fine; results carry NO numeric sentiment (probe re-confirmed live). Tone scored from headlines (this run: INLINE by Claude Opus, no `ANTHROPIC_API_KEY` in env — directionally equiv to the haiku path, headlines saved under `pilot/raw/` for reproducibility). `pilot/run_pilot.py` (snaps month-end sentiment dates to trading days; run with `$env:PYTHONPATH='.'`; yfinance installed locally, NOT in image) drove the engine. **RESULT (`pilot/RESULT-approx-2026-06-20.md`): pipeline CONFIRMED end-to-end; held-out 5d rank IC positive in level (+0.29) and delta (+0.33) but n≈18 ⇒ ~1.2 SE from zero, non-monotone quantiles ⇒ STATISTICALLY INCONCLUSIVE; all 3 names trended up (confound).** Recommendation (DECIDED): do NOT size off this, do NOT fan the MCP-approx version out further (cost without power/fidelity); the de-risked pipeline is the win. **Task 9 (faithful RavenPack REST, larger universe, multiple regimes) is the decisive run — still the only remaining work, gated on the business-email key.** Minor (d) fixture-themes is now moot (pilot is per-symbol); Minor (b) event_filtered still unwired (needs event-date sourcing).

**MINORS TRIAGE 2026-06-20 (later session, commit `31229c7` on origin/main; 308 tests):** fixed 3 of the open Minors — (e) `prices_yf` MultiIndex (new `_closes_from_frame` tolerates yfinance single-symbol MultiIndex cols), SentimentSeries immutability (`points` coerced to tuple in `__post_init__`), and (c) cross-ticker standardization (new `to_zscore` in align.py + opt-in `standardize` flag on `run_backtest`, default OFF so existing behavior/tests untouched; `report_markdown` discloses raw-levels vs per-ticker-standardized). Also fixed the `<a href>` scheme deferred-finding in `sanitise_html` (commit `b6d1adf`; see [[newsbrief-deferred-findings]]). **STILL-OPEN Minors:** (b) `event_filtered` wiring (deferred — needs event-date sourcing that only lands with Task 8, so premature now; user agreed), (d) fixture theme coverage (a DATA re-capture = Task 8 work, not a code fix). **Fork's quantile q<1 / hit_rate 0.0-vs-NaN nits FIXED 2026-06-20 (commit `af845b2`):** `quantile_returns` raises ValueError on q<1; `hit_rate` returns NaN (not 0.0) when no directional pairs, so "undefined" ≠ "0% agreement". TDD'd (tests/test_backtest_metrics.py).

**TESTING GOTCHA (non-obvious):** CI installs ONLY `requirements.txt` (feedparser, requests) + `requirements-dev.txt` (pytest, ruff) — **pandas is NOT in CI** (it's present locally because yfinance pulled it in once). Any `backtest/` test that needs pandas MUST guard with `pd = pytest.importorskip("pandas")` so it runs locally against real pandas and SKIPS in CI. Don't add an unguarded pandas import to a test module — it fails collection in CI.

**2. Capture live MCP fixtures to exercise the flag-on path end-to-end.** Validates Feature A with REAL data without a REST key. With the Bigdata.com MCP connector connected, call `find_securities` / `bigdata_sentiment_tearsheet` / `bigdata_events_calendar` (categories earnings-call AND conference-call) per universe symbol (cached ids: CVX=D54E62 MU=49BBBC RGLD=263216 ESLT=0401A0 AVAV=F1EB39), normalise each into the `symbol_<TICKER>.json` / `theme_<theme>.json` model shape, drop in a dir, then run with `ENRICHMENT_ENABLED=1 ENRICHMENT_PROVIDER=fixture ENRICHMENT_FIXTURE_DIR=<dir>` and eyeball the rendered prompt block + the persisted `enrichment-<date>.json` + the `bigdata_sentiment` signal annotations. This is the interim production-equivalent path until the business-email `BIGDATA_API_KEY` lands; flipping to `ENRICHMENT_PROVIDER=bigdata` + the key is then the only remaining change (and step 1 of [[bigdata-enrichment-shipped]]'s verify-at-enable list — confirm live REST field names — applies then).

Also still outstanding (prerequisite for true production, not blocking the above): obtain the business-email REST key (custom-domain email route noted in [[bigdata-evaluation-and-trading-split]]).
