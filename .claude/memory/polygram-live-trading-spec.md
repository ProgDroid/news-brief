---
name: polygram-live-trading-spec
description: "PolyGram real-money live-trading design — two-sleeve barbell spec, approved 2026-07-21, ready for implementation plans"
metadata: 
  node_type: memory
  type: project
  originSessionId: 35e03ada-b103-40f7-a138-5273bb11739f
  modified: 2026-07-27T09:36:24.998Z
---

2026-07-21: BRAINSTORM COMPLETE + SPEC APPROVED + FOUNDATION PLAN WRITTEN for taking **real money** live on polygram.ink prediction markets (equity/crypto stay PAPER). Spec `docs/superpowers/specs/2026-07-21-polygram-live-trading-design.md`; foundation plan `docs/superpowers/plans/2026-07-21-polygram-live-foundation.md` (6 TDD tasks: polygram_live.py write layer + execution/sleeve book fields + kill-switch/caps + fail-closed reconcile). FOUNDATION BUILT+REVIEWED (6 TDD tasks 37ba964..7df4e85, all clean inline, 604 tests green, BOM-free, PG_LIVE_ENABLED default off = inert). SLEEVE A PLAN WRITTEN+committed (2377d77, 7 TDD tasks). ALL THREE PLANS WRITTEN+committed (foundation 3bc4aa9, Sleeve A 2377d77, Sleeve B 5c8774c). Foundation BUILT+reviewed (6 tasks). **SLEEVE A BUILT+PUSHED 2026-07-22 (7 TDD tasks, inline exec, commits 062652f..03ee9a8; pushed d4c8ca2..03ee9a8 → origin/main = Docker deploy triggered — foundation was unpushed too, so this deploy carries foundation + Sleeve A together; full gate green = ruff clean + 618 tests).** Still INERT on host until PG_LIVE_ENABLED=1 + PG_A_ENABLED=1 + creds set.

**SLEEVE B BUILT+PUSHED 2026-07-22 (7 TDD tasks, inline exec, commits 962c813..05a44ec = 8 commits; PUSHED 03ee9a8..05a44ec → origin/main = Docker deploy triggered; full gate green = ruff clean + 630 tests, all subjects BOM-free; +2 fix commits beyond the 6 task-commits: cross-sleeve-cap 162dfa4 + effective-stake-cap 05a44ec).** Tasks: (1) thesis_log storage in common.py (load/save/append, locked); (2) PG_B config + `_sleeve_b_open_ok` cap/no-DCA guard; (3) SAFETY FIX `/close` routes execution==live → polygram_live.close_live_position (was paper-marking, orphaned real capital); (4+5) `/predict` wizard (landed together, coupled) — thesis→search→market→side→stake→hold→p̂→confirm→open+log, reuses `_WIZARD`/telegram_edit_text/`pr:` callback pattern; (6) score_settled_theses (outcome + Brier vs p̂) in mode_monitor; (7) resolution-aware prune_scored_theses in run_retention. THREE fixture-drift defects caught+fixed by TDD ([[tdd-plan-fixtures-drift-from-contracts]]): (a) Task-2 test caps arithmetic self-contradictory (amount 10 "within" yet 9 "over total" at exposure 20/cap 25 — fixed call-1 to 5.0); (b) Task-5 fake_open omitted "id" that real open_live_position always sets + _predict_commit reads (added faithful id); (c) added `import common` to brief.py (live kill-switch flags need module-attr access not frozen `from` copy). FINAL REVIEW FINDING (Important, plan-mandated) FIXED per user (chose cross-sleeve cap): _predict_commit passed live_exposure=0.0 → Sleeve B bypassed global PG_LIVE_TOTAL_CAP; now sums real open-live cost_basis across BOTH sleeves (matches open_sleeve_a_live), test-guarded (162dfa4). The stake-cap minor (presets/PG_B_POS_CAP exceeding PG_LIVE_PER_TRADE_CAP → confusing "venue rejected") FIXED 05a44ec: wizard now bounds presets + free-text to effective cap min(PG_B_POS_CAP, PG_LIVE_PER_TRADE_CAP), shows "max $N". Verified invariants: sweep_live_exits filters sleeve!="A" (Sleeve B hold-only, exempt from auto-exit); weekly MtM skips execution=="live"; double kill-switch; fail-closed spine. Ledger: .superpowers/sdd/progress.md. **FULL TWO-SLEEVE FEATURE (foundation+A+B) NOW COMPLETE, gated OFF.** NEXT = user pushes (Docker deploy) + go-live ops (fund polygram.ink, set caps+creds, flip PG_LIVE_ENABLED=1 → PG_A_ENABLED=1 → PG_B_ENABLED=1 after /predict validated, live-verify 3 deferred API points) → then spec #2 (machine-proposed theses). Two plan defects caught by TDD + fixed: (1) Task 3 test fixture `_parse_pg_market` lacked `question` → impl reads `parsed.get("question")` (topic label, safe since real parser always sets it); (2) Task 6 `trade_history` list-fallback referenced unbound `items` → returns `data`; (3) Task 7 test used `realized_return` but `_stats` scores `net_return` → test rewritten to net_return so the `execution!="live"` paper-gate guard is actually exercised. Also added `import polygram_live` to brief.py. Sleeve B (7 tasks) NOT YET EXECUTED. NEXT = EXECUTE Sleeve B plan; then user pushes (Docker deploy) + enables on host (PG_LIVE_ENABLED=1 PG_A_ENABLED=1 + POLYGRAM creds) + live-verifies the 3 deferred API points (eventId key, marketId acceptance, /trade/history field names). SAFETY BUG found by Explore + fixed in Sleeve-B Task 3: /close (_close_ticker/_close_picker_render in brief.py) paper-marks execution==live rows via _close_position_at_market -> orphans real capital at venue; Task 3 routes live rows to polygram_live.close_live_position (real sell). Sleeve B = /predict wizard (pr: callback namespace, reuses _WIZARD/_addsource pattern) + thesis_log.json (in common.py, shared w/ retention) + Brier scoring on settlement + resolution-aware retention. Sleeve A integration RESOLVED (via Explore agent): eventId from /search event wrapper (ev.get("id")/eventId — LIVE-VERIFY key name); marketId=existing row.instrument numeric id (LIVE-VERIFY trade API accepts it); tokenId=row.token_id; spread gate reuses trading._fetch_pg_half_spread (half-spread<=PG_A_SPREAD_GATE); exits run hourly in mode_monitor (sweep_live_exits: take/stop/time-stop, near-dated hold); live rows SKIP weekly mark_to_market measurement-holds; settled backfill via /trade/history (LIVE-VERIFY field names); paper-only go-live gate + separate live P&L line. **API contract VERIFIED** (docs pasted; captured in spec §1): base `https://polygram.ink/api`, JWT Bearer; `POST /trade/place` (market, synchronous fill w/ fillPrice+spreadFee+tradeFee), `POST /trade/sell {positionId}`, `GET /trade/positions` (reconcile + get positionId), `GET /wallet` (balance), `GET /orderbook/:tokenId` (live spread gate); USD custodial balance funded by Polygon USDC/USDT; min $1; 300/min authed. Decided: **market orders v0** + live-orderbook spread gate + record real fees to MEASURE drag → limit orders later only if fees prove to eat Sleeve-A edge (exits always market). Plumbing: place needs eventId+marketId+tokenId (book lacks eventId → capture at open); sell needs positionId from /trade/positions; settlement = position disappears from /trade/positions (no redeem call).

**2026-07-27 GO-LIVE DIAGNOSIS — every deferred API point now LIVE-VERIFIED; nothing was missing in the code.** User had flipped PG_LIVE_ENABLED/PG_A_ENABLED/PG_B_ENABLED (in the server's compose `environment:` block) and saw zero live trades. A throwaway read-only probe against the real funded account confirmed **all four guessed shapes are correct**: `POST /auth/login`→`token`; `GET /wallet`→`{"balance":"51.76…","currency":"USD"}` (parses, funded, clears caps); the `/search` event-wrapper id IS under `id` (so `ev.get("id") or ev.get("eventId")` in `_gather_pg_candidates` is right); markets DO carry JSON-encoded-STRING `outcomePrices`/`clobTokenIds`; `GET /orderbook/:tokenId`→`{"bids":[{"price":0.88,"size":…}],"asks":…}` (half-spread computed 0.0056 ≤ gate 0.03). The "live-verify 3 API points" NEXT item is **DONE** — no provider rewrite needed, unlike the bigdata case ([[bigdata-rest-api-verified]]).

**Real cause = gate asymmetry, not a defect.** Paper (`_open_prediction_positions`) opens on similarity≥0.60 + market open + price>0. Live (`open_sleeve_a_live`) *additionally* requires event_id + held price ∈[0.75,0.92] + half-spread≤0.03 + caps + funded wallet. Measured against the user's 69 real paper prediction entries (2026-06-25→07-27): **17 = 24.6% sit inside the band.** Do NOT estimate this from a raw `/search` catalogue sample — that gives ~1.5% and is badly over-pessimistic, because the matcher selects signal-relevant markets which cluster far nearer the band than the catalogue average (catalogue is bimodal: near-1 "crumbs" or ~0.5 coin-flips). At ~2 matches/day, ~1 in-band chance every other day; only ~2 had occurred since the flags were flipped, so "no trades yet" was fully consistent with a correctly working sleeve.

**Corollary — the paper book does NOT preview live behaviour.** Different gate set ⇒ different population. Any go-live gate calibrated on paper stats (hit rate, trade count, Sharpe) measures markets Sleeve A will never trade.

**Fixed same session** (1e86f08 compose passthroughs, ab79307 refactor+logging, 639 tests): (a) `_open_prediction_positions` and `open_sleeve_a_live` each ran their OWN `_gather_pg_candidates`+`run_prediction_matcher` — 2× search + 2× Claude call per collect, and since the matcher is nondeterministic the two passes could DISAGREE, so a paper row was no evidence Sleeve A ever saw that market; now one shared `_pg_match_pass(signals)->(candidates,matches)` computed in `mode_paper`, threaded into both (each keeps `match_pass=None` fallback). (b) Four of Sleeve A's eight gates returned 0 with no log line at all; now every early return logs its reason (with parsed flag values), per-match skips are tallied by reason (`below_similarity`/`no_event_id`/`already_open`/`unreadable_or_closed`/`side_missing`/`entry_gate`/`open_rejected`) and summarised in one line per run, and `_sleeve_a_entry_ok` logs price/band/half-spread/gate on rejection so the band can be tuned on real numbers. See [[env-var-needs-compose-passthrough]].

**Design = two-sleeve barbell** (edge thesis = differentiated-sourcing informational edge, "consistent alpha not massive profit", UNPROVEN → measure it):
- **Sleeve A — systematic favorite-fade:** buy the high-priced favorite side (band ~0.75–0.92) of tight-spread binaries; **hard `entry_spread` gate is THE highest-leverage rule**; exits = reprice-target OR stop OR time-stop, checked on the **monitor cron (frequent), NOT weekly MtM**; minimal fixed capped stake. Kills the paper measurement-holds (`PAPER_CLOSE_HORIZON`=4w, `PG_MAX_HOLD_DAYS`=182d) for LIVE rows — that was the "extending to measure performance" the user wanted gone.
- **Sleeve B — discretionary conviction hold (v0 user-authored):** new `/predict` Telegram wizard (reuse `_wizard_*`/`_handle_callback_query` from `/addsource`; DON'T overload existing `/thesis`); hold-to-settlement/until-`/close`, exempt from Sleeve A auto-exits; contained by zero-able per-position cap + total cap + **NO DCA** + separate P&L. This is where the sourcing edge (oil-Jan→Apr type slow-burn) lives.

**Key decisions:** sizing = fixed/minimal + hard caps (Kelly/confidence DEFERRED — need calibrated p_hat we haven't earned); new `polygram_live.py` write layer (place/close/reconcile, limit orders only, **fail-CLOSED** — trust venue over book); `execution:"live"` + `sleeve` fields on book.json; fix `source_kind:"unknown"` on prediction rows; volume monitor repurposed = liquidity/settlement watcher NOT entry signal.

**Calibration (starts day one, machine-generation DEFERRED to spec #2):** shadow-log EVERY thesis (score vs market-implied Brier-skill, discrimination-first); **resolution-aware retention** (keep payload until resolve_by+grace+scored, then prune to compact scored summary — NOT the flat `NEWSBRIEF_RETENTION_DAYS` sweep, which would delete unresolved theses); pre-registered kill/keep/promote gate (~1yr out, real-time-only, be patient). Machine-proposed theses (propose-then-confirm) + earned auto-open = spec #2, gated on the scorecard (see [[self-improving-trading-roadmap]]).

**Paper-data findings that drove it (n≈21 closed, one hot Hormuz window, directional not proof):** 9/9 closed resolution NO-favorite bets positive (favorite-longshot bias, NOT forecasting); disasters = cheap YES longshots (−97% to −100%); `entry_spread` up to 0.50 on longshots vs ~0 on favorites (spread is the #1 killer); repricing is fast/capturable and holding ate the −97% tail; median fade only +3.2% (thin → need Sleeve B for asymmetry). Clock confound cleared = fully contemporaneous. Relates to [[multi-asset-trading-build]] ("live exec = future want" now being built), [[polygram-candidate-search-fix]], [[sentiment-sizing-null-decided]] (why launch fixed-stake).
